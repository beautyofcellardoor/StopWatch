var expect = require('chai').expect;

var assert = require('assert');
describe('testing testing 1-2-3', function(){
  it('should pass when everything is okay', function(){
      expect(true).to.be.true;
  });
});
